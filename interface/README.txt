Each type has its own host and some other things. As we have to decide
which one to use due to the case, I've moved the codes to judge the type
here. Thus, when other parts need to import type-related configuration
files, just call interfaces defined here is okay.